# Integrations

## External SDKs & APIs
- **None explicitly identified** in the core codebase. The project relies entirely on Unity's built-in systems and standard packages.

## Unity Packages
- **TextMesh Pro**: Used for advanced text rendering in UI elements.
- **Visual Scripting**: Auto-generated folders (`Unity.VisualScripting.Generated`) suggest it is installed, though the primary logic appears to be C# driven.

## Analytics & Advertisements
- Not observed.

## Authentication & Database
- No external databases or backend services integrated. State and data (like `Leaderboard` or user progression) are either hardcoded, session-based, or potentially relying on PlayerPrefs (unverified).
